apiVersion: v1
kind: Service
metadata:
  name: auth-db
  namespace: dropmong-auth
  labels:
    app.kubernetes.io/part-of: medikong
    app.kubernetes.io/name: auth-db
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: auth-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: auth-db
  namespace: dropmong-auth
  labels:
    app.kubernetes.io/part-of: medikong
    app.kubernetes.io/name: auth-db
spec:
  serviceName: auth-db
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: auth-db
  template:
    metadata:
      labels:
        app.kubernetes.io/part-of: medikong
        app.kubernetes.io/name: auth-db
    spec:
      nodeSelector:
        medikong.io/workload: data
      containers:
        - name: postgres
          image: postgres:15
          args:
            - -c
            - max_connections=30
          ports:
            - name: postgres
              containerPort: 5432
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - user
                - -d
                - auth_db
            initialDelaySeconds: 5
            periodSeconds: 5
          env:
            - name: POSTGRES_USER
              value: user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-auth-credentials
                  key: password
            - name: POSTGRES_DB
              value: auth_db
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              memory: 256Mi
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        storageClassName: medikong-local-path
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: payment-db
  namespace: dropmong-payment
  labels:
    app.kubernetes.io/part-of: medikong
    app.kubernetes.io/name: payment-db
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: payment-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: payment-db
  namespace: dropmong-payment
  labels:
    app.kubernetes.io/part-of: medikong
    app.kubernetes.io/name: payment-db
spec:
  serviceName: payment-db
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: payment-db
  template:
    metadata:
      labels:
        app.kubernetes.io/part-of: medikong
        app.kubernetes.io/name: payment-db
    spec:
      nodeSelector:
        medikong.io/workload: data
      containers:
        - name: postgres
          image: postgres:15
          args:
            - -c
            - max_connections=30
          ports:
            - name: postgres
              containerPort: 5432
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - user
                - -d
                - payment_db
            initialDelaySeconds: 5
            periodSeconds: 5
          env:
            - name: POSTGRES_USER
              value: user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-payment-credentials
                  key: password
            - name: POSTGRES_DB
              value: payment_db
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              memory: 256Mi
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        storageClassName: medikong-local-path
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: coupon-db
  namespace: dropmong-coupon
  labels:
    app.kubernetes.io/part-of: medikong
    app.kubernetes.io/name: coupon-db
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: coupon-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: coupon-db
  namespace: dropmong-coupon
  labels:
    app.kubernetes.io/part-of: medikong
    app.kubernetes.io/name: coupon-db
spec:
  serviceName: coupon-db
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: coupon-db
  template:
    metadata:
      labels:
        app.kubernetes.io/part-of: medikong
        app.kubernetes.io/name: coupon-db
    spec:
      nodeSelector:
        medikong.io/workload: data
      containers:
        - name: postgres
          image: postgres:15
          args:
            - -c
            - max_connections=30
          ports:
            - name: postgres
              containerPort: 5432
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - user
                - -d
                - coupon_db
            initialDelaySeconds: 5
            periodSeconds: 5
          env:
            - name: POSTGRES_USER
              value: user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-coupon-credentials
                  key: password
            - name: POSTGRES_DB
              value: coupon_db
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              memory: 256Mi
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        storageClassName: medikong-local-path
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: order-db
  namespace: dropmong-order
  labels:
    app.kubernetes.io/part-of: medikong
    app.kubernetes.io/name: order-db
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: order-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: order-db
  namespace: dropmong-order
  labels:
    app.kubernetes.io/part-of: medikong
    app.kubernetes.io/name: order-db
spec:
  serviceName: order-db
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: order-db
  template:
    metadata:
      labels:
        app.kubernetes.io/part-of: medikong
        app.kubernetes.io/name: order-db
    spec:
      nodeSelector:
        medikong.io/workload: data
      containers:
        - name: postgres
          image: postgres:15
          args:
            - -c
            - max_connections=30
          ports:
            - name: postgres
              containerPort: 5432
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - user
                - -d
                - order_db
            initialDelaySeconds: 5
            periodSeconds: 5
          env:
            - name: POSTGRES_USER
              value: user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-order-credentials
                  key: password
            - name: POSTGRES_DB
              value: order_db
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              memory: 256Mi
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        storageClassName: medikong-local-path
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: interest-db
  namespace: dropmong-interest
  labels:
    app.kubernetes.io/part-of: medikong
    app.kubernetes.io/name: interest-db
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: interest-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: interest-db
  namespace: dropmong-interest
  labels:
    app.kubernetes.io/part-of: medikong
    app.kubernetes.io/name: interest-db
spec:
  serviceName: interest-db
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: interest-db
  template:
    metadata:
      labels:
        app.kubernetes.io/part-of: medikong
        app.kubernetes.io/name: interest-db
    spec:
      nodeSelector:
        medikong.io/workload: data
      containers:
        - name: postgres
          image: postgres:15
          args:
            - -c
            - max_connections=30
          ports:
            - name: postgres
              containerPort: 5432
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - user
                - -d
                - interest_db
            initialDelaySeconds: 5
            periodSeconds: 5
          env:
            - name: POSTGRES_USER
              value: user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-interest-credentials
                  key: password
            - name: POSTGRES_DB
              value: interest_db
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              memory: 256Mi
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        storageClassName: medikong-local-path
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
