loadtest:
  scenario: service-hpa-spike-load-test
  credentialsSecretName: read-api-loadtest-credentials

trafficModel:
  preset: ticket-110rps

scaleOutReport:
  enabled: true
  pollEveryIterations: 5
  rbac:
    create: true
  targets:
    - service: ticket-service
      namespace: ticketing-ticket

dataset:
  scenario: setup-capacity-baseline-dataset
  profile: capacity-baseline
  revision: capacity-baseline-half-year-early-growth-v2
  concerts: 270
  performancesPerConcert: 3
  seatSections: 1
  seatRows: 20
  seatsPerRow: 35
  customerPool:
    size: 100000
    emailPrefix: capacity-customer
    emailDomain: loadtest.medikong.local
    password: loadtest1234
    revision: capacity-baseline-half-year-early-growth-v2

manualRuns:
  dataset:
    activeDeadlineSeconds: 7200
  read:
    activeDeadlineSeconds: 1800

reportArchive:
  enabled: true
  size: 2Gi

resources:
  requests:
    cpu: 500m
    memory: 1Gi
  limits:
    cpu: "2"
    memory: 4Gi

scenarios:
  serviceHpaSpike:
    requestPrefix: loadtest-service-hpa-spike-ticket
    preAllocatedVUs: 250
    maxVUs: 700
    activeCustomerCount: 400
    serviceSteps:
      - ticket
    stages:
      - duration: 30s
        target: 30
        stageRole: warmup
      - duration: 90s
        target: 60
        stageRole: baseline
      - duration: 90s
        target: 90
        stageRole: spike
      - duration: 90s
        target: 110
        stageRole: overload
      - duration: 90s
        target: 60
        stageRole: cooldown
