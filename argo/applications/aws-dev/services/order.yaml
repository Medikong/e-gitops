apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: order-aws-dev
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/Medikong/e-gitops.git
      targetRevision: HEAD
      path: charts/medikong-service
      helm:
        releaseName: order-aws-dev
        valueFiles:
          - $values/values/base.yaml
          - $values/values/env/aws-dev.yaml
          - $values/values/services/order.yaml
          - $values/values/services/aws-dev/order.yaml
    - repoURL: https://github.com/Medikong/e-gitops.git
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: dropmong-order
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
