app:
  name: catalog-service
  namespace: dropmong-catalog
  labels:
    dropmong.io/service: catalog
    dropmong.io/tier: api

image:
  repository: catalog-service
  tag: ""

  digest: "sha256:35cdfb3d31653682dae2a7a685342608fc1bfde449aea565f4d5865a136cdf6c"
  version: "v0.1.4"
database:
  sqlalchemy:
    enabled: false

deployment:
  replicas: 2

container:
  port: 8081
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi
  env:
    - name: DATABASE_URL
      value: postgresql+asyncpg://user:password@catalog-db:5432/catalog_db
  readinessProbe:
    httpGet:
      path: /readyz
      port: http
    initialDelaySeconds: 3
    periodSeconds: 5
  livenessProbe:
    httpGet:
      path: /healthz
      port: http
    initialDelaySeconds: 10
    periodSeconds: 10

migration:
  enabled: true
  command:
    - python
    - -m
    - app.migrations
  args:
    - upgrade
  env:
    - name: DATABASE_URL
      value: postgresql+asyncpg://user:password@catalog-db:5432/catalog_db

service:
  port: 8081
  targetPort: http

ingress:
  enabled: false

networkPolicy:
  enabled: true
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kong
      ports:
        - protocol: TCP
          port: 8081

    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: istio-system
          podSelector:
            matchLabels:
              app: istio-ingressgateway
              istio: ingressgateway
      ports:
        - protocol: TCP
          port: 8081
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: catalog-db
      ports:
        - protocol: TCP
          port: 5432
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
          podSelector:
            matchLabels:
              app.kubernetes.io/name: opentelemetry-collector
      ports:
        - protocol: TCP
          port: 4317
        - protocol: TCP
          port: 4318
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: istio-system
          podSelector:
            matchLabels:
              app: istiod
      ports:
        - protocol: TCP
          port: 15012
        - protocol: TCP
          port: 15010
        - protocol: TCP
          port: 443
nodeSelector:
  medikong.io/workload: app

observability:
  profiling:
    serverAddress: http://pyroscope.observability.svc.cluster.local:4040
